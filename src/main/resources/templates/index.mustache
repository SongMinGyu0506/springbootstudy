{{>layout/header}}
    <h1>Starting Web Service to SpringBoot Ver.2</h1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <a href="/posts/save" class="btn btn-primary" role="button">글 등록</a>
                <!-- 머스테치는 다른언어와 같은 if문을 제공하지 않는다. true/false 여부만 판단
            그래서 머스테치에서는 항상 최종값을 넘겨줘야한다. 여기서도 역시 userName이 있다면 userName을 노출시키도록 구성-->
                {{#userName}}
                    Logged in as: <span id="user">{{userName}}</span>
                    <!-- Spring Security에서 기본적으로 제공하는 로그아웃 URL-->
                    <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                {{/userName}}
                <!--userName의 값이 false일 때 동작-->
                {{^userName}}
                    <!--Spring Security에서 기본적으로 제공하는 로그인 URL-->
                    <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>

                    <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>
                {{/userName}}
            </div>
        </div>
        <br>
        <table class="table table-horizontal table-bordered">
            <thead class="thead-strong">
            <tr>
                <th>게시글번호</th>
                <th>제목</th>
                <th>작성자</th>
                <th>최종수정일</th>
            </tr>
            </thead>
            <tbody id="tbody">
            {{#posts}}
                <tr>
                    <td>{{id}}</td>
                    <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                    <td>{{author}}</td>
                    <td>{{modifiedDate}}</td>
                </tr>
            {{/posts}}
            </tbody>
        </table>
    </div>
{{>layout/footer}}